use tp_individual::celda;

#[cfg(test)]
mod test_integracion {
    use crate::celda::Celda;
    #[test]
    fn test() {
        let algo = Celda::Vacio { representacion: '_' };
        assert!(true)
    }
}